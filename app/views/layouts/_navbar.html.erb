<header class="navbar navbar-fixed-top navbar-inverse">
	<div class="container">
		<nav>
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<div class="collapse navbar-collapse" id="myNavbar">
				<ul class="nav navbar-nav navbar-left">
					<li><%= link_to "Home", root_path %></li>
					<% if logged_in? %>
            <li class="nav-name"><%= current_user.name %> <%= "(Admin Status)" if current_user.admin? %></li>
					<% end %>
				</ul>
				<ul class="nav navbar-nav navbar-right">
					<li class="log-in"><%= link_to "log In", login_path, class: "btn btn-primary bttn" %></li>
					<li class="log-out"><%= link_to "log Out", login_path, method: :delete, class: "btn btn-danger bttn" %></li>
					<li><%= link_to "Create a Todo", new_todo_path %></li>
				</ul>
			</div>
		</nav>
	</div>
</header>